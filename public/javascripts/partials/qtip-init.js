$('a[title]').qtip({
	/*position: {
         target: 'mouse', // Use the mouse position as the position origin
         adjust: {
             // Don't adjust continuously the mouse, just use initial position
             mouse: false
         } 
     }*/
     //style: { classes: 'qtip-styling' }
});

/*$('.selector').qtip({
    style: { classes: 'qtip-styling' }
});*/